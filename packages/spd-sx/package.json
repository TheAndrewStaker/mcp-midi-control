{
  "name": "@mcp-midi-control/spd-sx",
  "version": "0.6.1",
  "description": "Roland SPD-SX hybrid descriptor: MIDI kit recall + pad triggers (AUDIO/MIDI mode) and mass-storage kit/wave authoring (WAVE MGR mode), driven by the unified tool surface.",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    "./*": "./dist/*"
  },
  "dependencies": {
    "@mcp-midi-control/core": "*",
    "@modelcontextprotocol/sdk": "^1.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  }
}
