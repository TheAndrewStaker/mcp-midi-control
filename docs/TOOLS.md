# MCP Tool Inventory

<!-- generated by scripts/list-tools.ts; do not edit by hand -->
<!-- regenerate with `npm run tools:inventory`; preflight enforces sync via tools:inventory-check -->

**Total registered tools:** 52.

## Summary

| Metric | Value |
|---|---|
| Total tools | 52 |
| Read (read-only) | 16 |
| Write (reversible) | 26 |
| Write (destructive) | 10 |
| Average description length | 798 chars |
| Tools over 600 chars | 29 |
| Tools over 1000 chars | 8 |

## All tools

Grouped by read/write classification, the same `readOnlyHint` / `destructiveHint` annotations a host (e.g. Claude Desktop's Manage Connectors page) uses to split read tools from write tools.

### Read (read-only)

No device state changes. `readOnlyHint: true`. Hosts group these as read tools.

| Tool | Description length | First sentence |
|---|---|---|
| `describe_device` | 1119 ⚠️ over 1000 | REQUIRED first call for any device question or apply_preset call. |
| `describe_rig` | 1510 ⚠️ over 1000 | Whole-rig overview: every device this server can drive, which are connected right now, and what each one is. |
| `find_compatible_types` | 613 ⚠ | Returns the subset of block.type values that expose EVERY knob you list (AND-semantics). |
| `get_param` | 761 ⚠ | Read one parameter from a device in display units (knob 0..10, dB, ms, %, enum name). |
| `get_params` | 434 | Batch-read parameters from a device. |
| `get_preset` | 1312 ⚠️ over 1000 | Snapshot the active working buffer: every placed block with current params in a PresetSpec-shaped envelope. |
| `import_songsterr` | 1281 ⚠️ over 1000 | Fetch a song's DRUM tab from Songsterr and return it ready for apply_pattern. |
| `list_midi_ports` | 579 | List every MIDI input + output port the OS exposes. |
| `list_packs` | 978 ⚠ | List the storage PACKS a device holds, by name. |
| `list_params` | 736 ⚠ | Enumerate a device's params with units and display ranges. |
| `list_pattern_recipes` | 253 | List the named drum-pattern library (four_on_the_floor, boom_bap, half_time, two_step, amen, trap_hats, …) for use with apply_pattern. |
| `list_samples` | 984 ⚠ | Read a device's named sample pool and return each slot's stored name (the sampler-archetype "what's in the pool" read; the names author_kit / the groove pack... |
| `lookup_lineage` | 966 ⚠ | Look up authored lineage for a block type: real hardware modeled, manufacturer notes, developer/forum quotes. |
| `measure_loudness` | 899 ⚠ | Measure real loudness (BS.1770 LUFS + true peak) from any OS audio input: a modeler's USB audio (the AM4 is a 4x4 interface), a mic, an interface bus. |
| `scan_locations` | 593 | Bulk-read stored preset names across a location range. |
| `translate_preset` | 980 ⚠ | Translate a preset between layout-class devices (AM4 / Axe-Fx II / III / FM3 / FM9). |

### Write (reversible)

Working-buffer edits and navigation, reversible by switching presets. Not destructive.

| Tool | Description length | First sentence |
|---|---|---|
| `edit_rig` | 806 ⚠ | Edit the configured OpenRig rig manifest (MCP_RIG_MANIFEST): add or remove a cable between two devices, or enable/disable one. |
| `export_preset` | 999 ⚠ | Back up a preset to a byte-exact file on disk. |
| `init_patch` | 412 | Reset the active patch to factory INIT state. |
| `reconnect_midi` | 812 ⚠ | You almost never need this. |
| `send_cc` | 602 ⚠ | Send a MIDI Control Change to any CC-responsive device. |
| `send_chord` | 243 | Play a chord: multiple simultaneous MIDI notes with a shared duration. |
| `send_clock_continue` | 201 | Send MIDI Timing Clock Continue (0xFB) to resume a stopped sequencer / drum machine from its current position. |
| `send_clock_start` | 278 | Send MIDI Timing Clock Start (0xFA) to start a sequencer / drum machine / clock-aware synth from the beginning. |
| `send_clock_stop` | 150 | Send MIDI Timing Clock Stop (0xFC) to halt a running sequencer / drum machine / clock-aware synth. |
| `send_note` | 213 | Play one MIDI note on any note-responsive device (synth, drum pad, sampler). |
| `send_nrpn` | 237 | Write an NRPN on any NRPN-responsive device. |
| `send_panic` | 251 | MIDI panic: silence every stuck note on every channel of a device. |
| `send_program_change` | 473 | Switch patches on any PC-responsive device. |
| `send_reset_controllers` | 243 | Reset All Controllers (CC 121) on a channel: pitch bend, mod wheel, expression, channel pressure, etc. |
| `send_sequence` | 321 | Play a timed sequence of MIDI notes (arpeggios, riffs, test patterns). |
| `send_song_position` | 252 | Send MIDI Song Position Pointer (0xF2): jump a sequencer / drum machine to a specific beat. |
| `set_block` | 698 ⚠ | Place ONE block, or clear a slot (block_type "none"), on a single preset. |
| `set_bypass` | 381 | Silence (bypassed=true) or activate (bypassed=false) a block on the currently-active scene. |
| `set_macro` | 425 | Set a Macro control (CCs 16-23 on the Hydrasynth; CC range varies by device). |
| `set_macro_route` | 998 ⚠ | Assign one of a performance Macro's (1-8) destinations by NAME on the Hydrasynth macro page (up to 8 each); allocates a free slot and writes target + depth +... |
| `set_mod_route` | 737 ⚠ | Wire one modulation-matrix route by NAME on a synth with a mod matrix (e.g. |
| `set_param` | 931 ⚠ | Write one parameter by (block, name) in display units (knob 0..10, dB, ms, %, enum name or wire index). |
| `set_params` | 559 | Batch-write parameters on a device. |
| `set_system_param` | 374 | Set a device-level system CC (master volume, sustain pedal, expression, mod wheel, all-notes-off). |
| `switch_preset` | 433 | Load a stored preset into the working buffer. |
| `switch_scene` | 344 | Change the active scene within the current preset. |

### Write (destructive)

Persists, overwrites a stored location, or sends raw bytes. `destructiveHint: true`.

| Tool | Description length | First sentence |
|---|---|---|
| `apply_patch` | 3776 ⚠️ over 1000 | Build a Hydrasynth patch atomically: apply a sparse override map (or recipe_id) on top of the factory INIT buffer and dump via SysEx to the named slot. |
| `apply_pattern` | 3259 ⚠️ over 1000 | Realize a drum / step pattern on a sequencer target (Circuit Tracks drums, Roland SPD-SX, or any voice device with a voice_map). |
| `apply_preset` | 1084 ⚠️ over 1000 | Build or replace the entire working-buffer preset in one call. |
| `author_kit` | 2033 ⚠️ over 1000 | Author a sampler KIT (a pad-to-wave map) and write it to a stored kit location. |
| `import_preset` | 694 ⚠ | Re-apply a byte-exact preset backup (a `.syx` written by export_preset) to the device. |
| `save_preset` | 974 ⚠ | DESTRUCTIVE: persist the working buffer to a stored location, optionally renaming first. |
| `send_sysex` | 316 | Send a raw SysEx frame. |
| `upload_kit` | 998 ⚠ | Upload a folder of WAVs to consecutive Circuit Tracks drum-sample slots in one batch. |
| `upload_project` | 985 ⚠ | Upload a prepared whole-project Circuit Tracks .ncs to a project slot, sent VERBATIM over the file-transfer transport. |
| `upload_sample` | 994 ⚠ | Add a WAV to a device sample pool. |

## Description budget outliers

Tools with descriptions over 1000 chars. Migration to structured response fields (via `describe_device.agent_guidance`) is the planned trim path; each override carries an inline reason in `scripts/list-tools.ts`.

| Tool | Description length |
|---|---|
| `apply_patch` | 3776 chars |
| `apply_pattern` | 3259 chars |
| `author_kit` | 2033 chars |
| `describe_rig` | 1510 chars |
| `get_preset` | 1312 chars |
| `import_songsterr` | 1281 chars |
| `describe_device` | 1119 chars |
| `apply_preset` | 1084 chars |
